#include <stdio.h>
int main() {
    int n;
    printf("enter the numer :");
    scanf("%d",&n);
    if(n%2==0)
    printf("the given nr %d is even",n);
    else
    printf("the given nr %d is odd",n);
}
