#include <stdio.h>
int main() {
    int n=80+40;
    printf("\nlength of the train and bridge is 40m and 80m respectively\n");
    printf("distance to be travelled by the train is %dm",n);
    return 0;
}

